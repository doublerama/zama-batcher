{
  "name": "zama-dca-batcher",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "HARDHAT_COMPILE=true hardhat deploy --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.4",
    "hardhat-deploy": "^0.12.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.3",
    "dotenv": "^16.4.1"
  }
}